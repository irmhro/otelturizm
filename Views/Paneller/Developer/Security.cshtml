@model otelturizmnew.Models.Paneller.User.UserSecurityPageViewModel
@{
    Layout = "~/Views/Paneller/Developer/_DeveloperPanelLayout.cshtml";
    ViewData["Title"] = "Developer Guvenlik";
}

@if (TempData["DeveloperSuccess"] is string successMessage)
{
    <div class="panel-inline-status success">@successMessage</div>
}

@if (TempData["DeveloperError"] is string errorMessage)
{
    <div class="panel-inline-status error">@errorMessage</div>
}

<section class="form-stack">
    <form method="post" asp-action="ChangePassword" class="panel-card">
        @Html.AntiForgeryToken()
        <div class="section-head"><h2>Sifre Guncelle</h2></div>
        <div class="form-grid">
            <label><span>Mevcut Sifre</span><input name="CurrentPassword" type="password" autocomplete="current-password" /></label>
            <label><span>Yeni Sifre</span><input name="NewPassword" type="password" autocomplete="new-password" /></label>
            <label class="full"><span>Yeni Sifre Tekrar</span><input name="ConfirmPassword" type="password" autocomplete="new-password" /></label>
        </div>
        <div class="save-row"><button type="submit" class="primary-btn">Sifreyi Guncelle</button></div>
    </form>

    <form method="post" asp-action="SaveTwoFactor" class="panel-card">
        @Html.AntiForgeryToken()
        <div class="section-head"><h2>Iki Asamali Dogrulama</h2></div>
        <label class="toggle-row no-border">
            <div><h3>2FA korumasi</h3><p>Developer hesabi icin e-posta veya telefon kodu ile ikinci adim kullan.</p></div>
            <input name="Enabled" type="checkbox" value="true" @(Model.TwoFactorEnabled ? "checked" : null) />
        </label>
        <input name="Enabled" type="hidden" value="false" />
        <div class="form-grid" style="margin-top:18px;">
            <label class="full">
                <span>Dogrulama kanali</span>
                <div class="radio-stack">
                    <label class="radio-option">
                        <input type="radio" name="Channel" value="email" @(Model.SelectedTwoFactorChannel == "email" ? "checked" : null) />
                        <span><strong>E-posta guvenlik kodu</strong><small>@(Model.EmailUsableForTwoFactor ? $"Aktif adres: {Model.MaskedEmailAddress}" : "Once e-posta dogrulamasi tamamlanmali.")</small></span>
                    </label>
                    <label class="radio-option">
                        <input type="radio" name="Channel" value="whatsapp" @(Model.SelectedTwoFactorChannel == "whatsapp" ? "checked" : null) />
                        <span><strong>Telefon / WhatsApp kodu</strong><small>@(Model.PhoneUsableForTwoFactor ? $"Dogrulanmis numara: {Model.MaskedPhoneNumber}" : "Telefon altyapisi gelistiriliyor, dogrulanmis numara gerektirir.")</small></span>
                    </label>
                </div>
            </label>
        </div>
        <div class="save-row"><button type="submit" class="primary-btn">Guvenlik Tercihini Kaydet</button></div>
    </form>
</section>
